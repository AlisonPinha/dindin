# Suprimir warnings de deprecação de dependências transitivas
# Esses warnings são de dependências indiretas (dependências de dependências)
# e não podem ser corrigidos sem atualizar o Next.js para versão 15+
legacy-peer-deps=false
